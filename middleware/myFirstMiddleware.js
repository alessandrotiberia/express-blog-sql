/*function myFirstMiddleware(request, response, next) {
    next(); // procedi
    
    
}
export default myFirstMiddleware;
*/
// su server usa app.use(myFirstMiddleware) e passa alle altre use 

// qquesta è globale ma c'è anche per path
// app.use ('/pizzas/margherita', myFirstMiddleware);